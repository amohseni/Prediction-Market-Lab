# PROGRESS — Prediction Market Reliability Model

## Planned
- **All 8 handoff milestones complete.** Remaining optional enhancements (not in the §8 spec):
  - Tab 3 calibration-curve + favorite–longshot sub-panels (need the ensemble to return reliability-diagram data per sweep point).
  - Cancel button for long sweeps/maps (needs future/promises for async).
  - Deploy to shinyapps.io.
- Formalization v1.0 (full spec per FORMALIZATION_GUIDE)
- Pseudocode stage
- Derive closed forms: one-shot wealth-weighted price, expected Brier via (θ, η) quadrature, n_eff^w
- Implementation (R/Shiny, modularized: core / Market Lab / Ensemble Analysis)
- Validation suite = §5 known results
- Adaptive sweeps for §6 interactions

## Done
- 2026-07-14: **UI refinements (from user review).** (1) Sidebar control labels now read text-first ("Traders \\(n\\)" not "\\(n\\) — traders"), description sentence-cased, symbol typeset. (2) All math in the Guide is MathJax: glossary definitions carry LaTeX (AE formula, n_eff, Kelly, LMSR cost, b·ln2, etc.) + a DT `drawCallback` re-typesets after each draw/search; How-it-works prose converted to inline LaTeX throughout. (3) `theme_pm()` gained 10pt title/subtitle bottom margins so the vertically-centered y-axis title can't bump into the title/subtitle. Verified live on all three.
- 2026-07-14: **Milestone 8 — Tab 5 Guide + polish (APP COMPLETE).** `R/mod_guide.R`: three sub-sections in a nested `navset_underline` — **How it works** (readable account of plan §1, HTML prose with bold terms), **Glossary** (47 terms from plan §14 verbatim, searchable `DT::datatable`), **References** (13 items, plan §13). Polish: cross-tab "See the anatomy of this run →" link on the post-resolution card (client-side JS clicks the Run Anatomy nav-link — verified switches tabs); guide-prose CSS. Captions/empty-states/exports/stale-badges already in place from earlier milestones; app loads clean (no console errors, ~1s). README updated to reflect the finished five-tab app. All 8 handoff milestones done; 10 unit test blocks green; V1–V6 validation (5 PASS, V4 honest REVIEW).
- 2026-07-14: **Milestone 7 — Tab 4 Interactions + V4–V6.** Engine: `sweep_2d()` + `hanson_oprea_effect()` in core_ensemble.R; `dist_star`/`signed_dist` ensemble metrics; `bot_pistar_random` (per-run random bot target) in the model (inert by default). `R/interaction_plots.R` (viridis heatmap + contours, click-to-slice, friction-ranking bars) + `R/mod_interactions.R`: 5 curated questions (echo-chamber wealth ρ×α_w→AE; manipulation frontier B_m×τ→distortion w/ frontier contour; fee optimum τ×r_wp→Brier; manufactured cascades h×B_m→persistence w/ bot rounds 3–8; friction ranking = HO bars) + free x/y/metric, cached, progress, CSV/PNG, stale note. Default map grid 5×5, R=50 (~15 s; noted maps are slow). Verified live: q1 AE heatmap + click-slice, q5 friction bars. **V4–V6 in validate.R → VALIDATION.md:** V5 favorite-longshot **PASS** (noise compresses prices → steep calibration; 0.36 forecast wins ~1%, 0.64 wins ~95%). V6 herding cascade **PASS** (bot rounds 3–8 leaves ~2.5× larger persistent distortion with h=0.4 vs h=0). **V4 Hanson–Oprea REVIEW (honest negative):** searched sleepy-market regimes (c_part∈{.3,.6}, σ_ε∈{.5,1}, bot exits after 3–8 rounds, random target, paired R=400) — bot **always significantly raises** Brier; its distortion outweighs the participation it provokes, and τ makes it worse. Notable: manipulation adds noise rather than waking the market. 10 unit test blocks green.
- 2026-07-14: **Milestone 6 — Tab 3 Reliability (1-D sweeps).** `R/mod_reliability.R` + `R/reliability_plots.R`. Setup strip (sweep param dropdown from the control spec, From/To auto-set to the param's range, Points, R, extra-metric checkboxes, Run). Opens on the **bundled n_eff-ceiling exhibit** (`scripts/make_exhibit.R` → `app/data/neff_exhibit.rds`, 1 KB, loaded as `PM_NEFF_EXHIBIT`) so the tab is never blank. A sweep replaces it: Brier headline with 95% CI ribbon + prior / best-possible reference curves + dashed static-benchmark overlay; Murphy decomposition (REL/RES/UNC); selected extra metrics (AE/log/bias/REL/RES) as free-y facets. Uses existing `sweep_1d()` + digest cache (re-runs instant), `withProgress` progress bar, CSV/PNG `downloadHandler`s. Stale handling: per-tab note + sidebar badge; running a sweep calls `stale(FALSE)` — this fixes the earlier badge-semantics concern (badge now = "settings changed since the last sweep"). Verified live: exhibit renders, ρ sweep shows best-possible rising toward the market as ρ grows (AE→1), Murphy + AE panels render, stale note fires on preset change. **Deferred to M8:** calibration-curve + favorite–longshot sub-panels (need ensemble reliability-diagram data), sweep Cancel (needs future/promises). 10 test blocks green.
- 2026-07-14: **Milestone 5 — Tab 2 Run Anatomy.** Diagnostics of the *current* run: `mod_live_server` now returns `reactive(rv$traj)`, wired into `mod_anatomy_server` so both tabs dissect the same market (auto-updates on sidebar/intervention changes). `R/anatomy_plots.R` (pure): convergence |p_t−p*| per round (log y), aggregation check (price vs wealth-weighted belief, ink/green — new `PM_COL$benchmark`), participation stacked bars (traded / no-trade band / never entered — verified differentiating under Sleepy), belief-migration lines (cascade toward price; gated on h>0 with a friendly empty-state when h=0), volume per round (scaled $). **Fixed a real bslib bug:** plots in a hidden→shown tab render at 0/tiny width ("invalid quartz() device size" / oversized text) — added a `shown.bs.tab → window resize` dispatch in `ui.R` so Shiny re-measures; verified all 5 plots then render at correct widths (962/1972px) with no errors. 10 test blocks still green.
- 2026-07-13: **Live Market refinements (from user testing).** (1) Resolution label right-aligned at the terminal step so it extends left, never cropped. (2) Speed recast as trades-per-tick (1..n, slider max tracks the n slider); animation now reveals per trade (finer) at a fixed ~90 ms tick. (3) Money scaled ×1000 for display feel (`PM_MONEY_SCALE`, `pm_money()`): wallet $10,000, amount-to-spend in scaled dollars (÷scale into the model), P&L table in scaled currency. Verified live: resolution label visible, per-trade animation advances, buy mid-run and at end both deduct correctly ($10,000→$8,000, +4–5 YES).
- 2026-07-13: **Milestone 4 — Tab 1 Live Market.** Engine: extended `run_market` with an optional interactive **user account** (`user_wallet` + scheduled `user_trades`; `user_trade()` reuses the Sec. 1.4 LMSR math, no Kelly) tracked in money conservation — inert (zero overhead) for ensembles/tests. Animation model = **precompute + replay, no resumable engine**: because a run is deterministic in `seed`, scheduling the bot (`bot_rounds = from:T`) or a user trade and re-running reproduces the identical revealed prefix + correct divergent suffix. `R/live_engine.R`: `pm_live_run` + plot/log/pnl helpers (price path w/ 2 color modes, agent swarm, event log, P&L-by-type). `R/mod_live.R`: full tab — control strip (Play/Pause/Step/New market/speed/scrub), price plot, bot card (toggle + budget + target + red status dot), wallet card (cash/position + Buy YES/NO), swarm, collapsible event log, post-resolution card. New unit test: user-account conservation + prefix-determinism (**10 blocks green**). **Bugs found & fixed in preview:** (1) slider-animation race — moved the reveal index to a server-side `rv$pos` source-of-truth, slider is display/manual-scrub only; (2) all actionButton `observeEvent`s fired on init (default `ignoreInit=FALSE`) → auto-play + phantom trades on load → added `ignoreInit=TRUE`. Verified live: no auto-play, per-round animation advances, bot shows red segments, user Buy YES deducts correctly ($10→$8, +4 YES), post-resolution P&L sensible (bot loses manipulating, informed profit).
- 2026-07-13: **House-style pass (milestone 3 polish).** Reverse-engineered the visual language from the user's other Shiny apps (grant-funding `app.R`, `Optimal-Strategy-for-First-Price-Sealed-Bid-Auction`). Rules: one sans (Helvetica Neue system stack, no web font), text only near-black/dark-gray, hue reserved for plots, very light gray UI panels. Changes: dropped Inter → Helvetica Neue; `primary` → dark gray so no blue chrome; re-skinned sliders/tabs/accordion/links to grayscale; three-column précis in a light `#f8f9fa` panel; `theme_pm()` aligned to house `theme_sim()`. Rewrote `pm_app_css()` with token substitution (no fragile positional sprintf). Per user review: removed décor top-rules over précis columns + the sidebar `hr`; new title "When are prediction markets reliable?" + subtitle; switched `navset_tab`→`navset_underline` and dropped redundant per-tab card headers (tab content is a `.pm-tab-body` div); unified active-tab color/weight with sidebar section titles (`#212529`/600); **all math symbols now LaTeX via MathJax** — control labels carry `sym`(LaTeX)+`desc`, group details use inline `\\(...\\)`, `withMathJax()` added. Verified in preview (34 MathJax nodes, no raw TeX; tab color == accordion header).
- 2026-07-13: **Repo + V1 decision.** Project moved into its own public GitHub repo (https://github.com/amohseni/Prediction-Market-Lab, MIT); app under `app/`, spec + design docs under `docs/`. V1 review decision (option a): retired the 0.03 mad target — the ~0.067 gap is irreducible unbiased realization scatter; V1 now PASS on corr≈0.97 + ~0 bias. Applied consistently in `validate.R`, `docs/CLAUDE_CODE_HANDOFF.md` §7.2, and `app/VALIDATION.md`.
- 2026-07-13: **Milestone 3 — shell app (loads in ~1 s, target < 3 s).** `app/global.R` (bootstrap: libs, deterministic R/ sourcing with autoload off, constants + précis copy), `ui.R` (title → 3-column explainer → `layout_sidebar` + 5-tab `navset_tab`; sidebar built from a single control spec), `server.R` (one shared `params()` reactive, preset/reset wiring, lesson caption, stale badge threaded to modules). New `R/presets.R` (6 presets §5 + `pm_controls()` sidebar spec, 20 controls with exact GUI captions), `R/mod_{live,anatomy,reliability,interactions,guide}.R` shells, `R/theme.R` gained `pm_app_css()` + `pm_tab_placeholder()`. `.claude/launch.json` (`pm-lab`, port 8200). **Verified via preview:** app boots clean (no console/server errors), sidebar opens with all 20 sliders, accordion is one-open-at-a-time, preset switch (Echo chamber → ρ=0.5) updates sliders + lesson + flips stale badge, Reset restores Textbook defaults, all 5 tabs render. bslib sidebar needed `open="open"` to start expanded (noted).
- 2026-07-13: **Milestone 1 — model core + unit tests (all green).** `app/R/core_model.R` (pure, no Shiny): closed-form beliefs (prior/informed posterior/omniscient/n_eff), LMSR (price/cost potential C/buy-YES-NO/budget), `agent_turn` (9-step Sec. 1.5 rule; manipulators = frozen-belief agents; dormant agents re-check entry every turn, entry one-way), `run_market` (draw world → T rounds → resolution; full/light record modes; audit hook). `app/R/theme.R` = color constants + P_MIN/P_MAX + one ggplot theme. Tests 1–7 (`tests/testthat/`, runner `scripts/run_tests.R`): LMSR consistency + exact reference values, operator loss ≤ b·ln2, posterior vs adaptive-quadrature Bayes (1e-6), n_eff vs 1ᵀΣ1, Kelly optimality, money conservation every step (1e-8) with all frictions+bot, determinism, Murphy identity B=REL−RES+UNC+(WBV−2·WBC) exact. **Perf:** inlined the hot LMSR scalar math in `agent_turn` (no list allocs / plogis wrappers) → default sweep point R=200,n=100,T=20 in ~2.0 s (was 16 s), meeting the ≲2 s target. Single turn code path preserved.
- 2026-07-13: **Milestone 2 — ensemble + metrics + validation.** `app/R/core_ensemble.R`: `pm_brier`, `murphy_decomposition`, `accuracy_eff` (AE), `log_score`, `calibration_fit`, `run_ensemble` (per-run table + summary; participation & active-fraction metrics), `sweep_1d` (mean±95% CI, reference lines, static benchmark), digest-keyed in-memory cache. `scripts/validate.R` → `app/VALIDATION.md` + figures. **Tuning decisions (my discretion per handoff §0.4):** V1 wealth-weighting — corr(p_T,p_static)=0.978 with ~0 bias (lesson PASS); but mean|gap|≈0.067 exceeds the 0.03 target and is *irreducible* (flat across T=50/80/120 and n=50..800) structural scatter of one market's price around the wealth-weighted fixed point → marked **PARTIAL**, flagged for review (0.03 unattainable under sequential-Kelly). V2 n_eff ceiling — clearest in the achievable frontier B_omn (drop n=25→800: 0.050 at ρ=0 vs 0.005 at ρ=0.5); market Brier improves only modestly because a single market does wealth-weighted opinion pooling, not Bayesian signal pooling (reported as a stronger form of the same ceiling). V3 fees — needed tau range extended to [0,0.8] (fees are partial/self-taxing per §1.4); Brier rises ~0.19→0.23 and active fraction collapses 1.0→0.03.
- 2026-07-13: CLAUDE_CODE_HANDOFF.md — binding implementation spec. Full closed-form model (posteriors, LMSR trade/cost/budget formulas — all numerically verified; AgentTurn algorithm with fee/participation gates; manipulators as frozen-belief agents), file structure, binding GUI spec, 6 presets, color constants, 7 exact unit tests with reference values + 6 emergent validation checks, 8 build milestones, pitfalls list. Formalization + pseudocode stages folded into the handoff.
- 2026-07-13: All remaining decisions resolved. R1: herding IN for v1 (belief-adoption, default h = 0) — model design closed, plan §11 has no open items. GUI: Run Anatomy = own tab; price-path coloring = two-mode toggle (default: red = bot/structural manipulators, orange = user; opt-in color-by-trader view; swarm & log always type-colored). Docs updated (plan v0.3 final, GUI_DESIGN §4/§5/§7).
- 2026-07-13: GUI_DESIGN.md v0.1 — full content inventory (explanatory / single-run / ensemble / cross-cutting state); single-interface layout (title → 3-column explainer → sidebar with preset scenarios + 5 accordion parameter groups with one-line captions → 4 working tabs + Guide); visual-language table (ink price / gray prior / blue omniscient / red manipulation, consistent across tabs); interaction architecture (precompute-and-replay animation with intervention injection, stale badges, cached ensembles, never-blank initial state).
- 2026-07-13: v0.3 — decisions integrated: binary contract; LMSR-only v1 (+ new §1.2 analysis of what changes under an order book); participation margin in; repeated markets cut (dependent material marked deferred); ground truth = joint + conditional; manipulator bot with red-highlighted price segments; R/Shiny both modes. New §1.4 "Three frictions, three mechanisms" (τ = intensive-margin shrinkage, κ = ex-post censoring, c_part = ex-ante sample selection; Hanson–Oprea from fees alone is partial and self-taxing; conjectured ordering c_part > κ > τ, added as §6 item 7). Herding section expanded (transient-blend vs belief-adoption variants; fixed point unchanged under blend, cascades require adoption); recommended for v1, pending confirmation (R1).
- 2026-07-13: v0.2 rewrite for clarity — plain-language prose throughout, every term of art defined at first use, added §14 Index of terms (alphabetical glossary). Content unchanged.
- 2026-07-13: Design document v0.1 (PREDICTION_MARKET_MODEL_PLAN.md): model sketch, parameter table, reliability-factor inventory, metrics (Brier + Murphy decomposition, AE, manipulation robustness), validation targets, six candidate interaction effects, initialization survey, open questions. Literature check: Wolfers–Zitzewitz, Beygelzimer–Langford–Pennock, Hanson–Oprea, Kets et al.; Polymarket/Kalshi CLOB initialization practice.
